.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# secrets — never commit
.env
.env.local

# generated artifacts
datasets/dev/
datasets/holdout/
datasets/challenge/
traces/
.chroma/

# evals/runs/ is deliberately NOT ignored: the per-case records are the evidence
# behind every number quoted in the reports, and a claim whose backing artefact
# was thrown away is not reproducible.

# editors
.idea/
.vscode/
